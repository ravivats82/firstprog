---
- name: Test code to display variables
  hosts: localhost
  gather_facts: no
  become: true
  become_user: testuser

  tasks:
    - name: ping cluster
      command:
        ping -c 1 {{ cluster }}
      register: cluster_ping
