---
- name: Test code to display variables
  hosts: localhost
  gather_facts: no
  become: false

  tasks:
    - name: ping cluster
      command:
        whoami
      register: cluster_ping
      ignore_errors: yes
