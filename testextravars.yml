---
- name: Test code to display variables
  hosts: localhost
  gather_facts: no
  become: false

  tasks:
    - name: ping cluster
      command:
        /bin/ping -c 1 {{ cluster }}
      register: cluster_ping
